근본 원인 1: PyPDFLoader의 표 처리 한계
→ 표 구조(행/열)가 사라지고 텍스트로 뭉개짐

근본 원인 2: chunk_size=300 고정 크기 청킹
→ 표 데이터가 의미 단위로 잘리지 않고
   크기 기준으로 잘림
→ "1종 + 복잡추나 + 30%" 가 다른 청크로 분리됨

근본 원인 3: 이 두 문제가 합쳐지면
→ 정답 근거가 어느 청크에도 온전히 없음
→ 검색해도 정답 못 찾음